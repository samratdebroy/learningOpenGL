#version 440 core

layout (location = 0) in vec3 position;

out vec4 vertexColor; // Specify a color output to the fragment shader

void main()
{
	gl_Position = vec4(position, 1.0);
	vertexColor = vec4(0.5f,0.0f,0.0f,1.0f); // set output to dark-red color
}
